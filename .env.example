# Media & Doc Studio — Environment Variables
# Copy this file to .env and fill in your values
# Never commit .env to GitHub

# ── Backend API (Phase 1 — leave blank for now) ──
BACKEND_API_URL=
BACKEND_API_KEY=

# ── Analytics (Phase 1 — leave blank for now) ──
ANALYTICS_ENABLED=false

# ── Development ──
LOG_LEVEL=INFO
